# T<nn>: <short title>

- Slug: `<slug>`
- Size: S | M
- Depends on: T<nn> | none
- PRD: `docs/planning/<slug>/prd.md`
- Spec: `docs/planning/<slug>/spec.md`

## Goal

One paragraph. What done looks like for this slice.

## Acceptance criteria

- [ ] …
- [ ] …

## Likely files

- `path/to/file`

## Validation (do this before calling the ticket done)

- Unit: `<command>`
- Integration / E2E (if any): `<command>`
- Manual: …

Write failing tests first when implementing this ticket (skill `validation-tdd`). Ship still goes through `no-mistakes`.
