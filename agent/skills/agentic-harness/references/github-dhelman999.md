# GitHub identity (David Helman)

Account: **[dhelman999](https://github.com/dhelman999)**

## Verified local setup

- `gh` logged in as `dhelman999` (keyring); scopes include `repo`
- Git protocol: **SSH**
- `git` user.name: David Helman
- `git` user.email: dhelman999@gmail.com

## How to use in future tasks

| Task | Approach |
|------|----------|
| List repos | `gh repo list dhelman999` |
| Clone | `gh repo clone dhelman999/<repo>` or `git clone git@github.com:dhelman999/<repo>.git` |
| Create repo | `gh repo create dhelman999/<name> ...` (only when user asks) |
| Commit | Only when user explicitly asks; follow user git/PR rules |
| Push | Only when user explicitly asks; no force-push to main/master unless requested |

## Known public repos (as of setup)

- McWendyQueen (Java)
- Pong, Kewl-Tetris (Unity)
- DMUD (C#)
- cisco-mibs (fork)

Do not change `git config` globally unless the user asks.
