List every skill available in this harness with a **one-line** name + summary.

Scan `C:\Projects\dev-hub\agent\skills\*\SKILL.md` (includes personal junctions). Read each frontmatter `name` and `description`; compress description to one short line. Skip `hide-from-catalog: true` (do not name skipped skills). Do not scan `dev-hub-personal` to re-add them. Sort by name. Output a compact bullet list only — no skill body dumps.

Same behavior as `/skills` (skill `skills`).
