List every skill available in this harness with a **one-line** name + summary.

Scan `C:\Projects\dev-hub\agent\skills\*\SKILL.md` (includes personal junctions). Read each frontmatter `name` and `description`; compress description to one short line. Sort by name. Output a compact bullet list only — no skill body dumps.

Same behavior as skill `/skills`.
