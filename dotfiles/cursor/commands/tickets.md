# /tickets

Slice the accepted PRD + spec into sprint-sized tickets with ACs, verification commands, and a dependency graph.

Follow skill **`slice-tickets`**. Explicit invoke only. Same as `/slice-tickets`.
