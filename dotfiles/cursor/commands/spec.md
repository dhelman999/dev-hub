# /spec

After an accepted PRD: 2-3 approaches with trade-offs, recommend, wait for their call, then write `docs/planning/<slug>/spec.md`.

Follow skill **`spec`**. Propose; do not dictate. Explicit invoke only.
