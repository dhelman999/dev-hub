# /prd

GATE-style PRD interview (initiate → foundation → deep dive → hypothesis → MVP), then write `docs/planning/<slug>/prd.md`.

Follow skill **`prd`**. End the turn after each cluster. Do not invent requirements. Explicit invoke only.
