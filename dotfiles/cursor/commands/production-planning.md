# /production-planning

Opt-in production-planning. Do not volunteer this. Do not auto-pipeline.

1. Follow skill **`production-planning`**.
2. Start skill **`prd` at Phase 1** (restate + GATE). Do not write spec or tickets in the kickoff turn.
3. After they accept the PRD, **offer** `/spec`. After they accept the spec, **offer** `/tickets`.
4. Artifacts under `docs/planning/<slug>/`. Do not implement unless they already asked to continue.
