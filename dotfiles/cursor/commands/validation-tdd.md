# /validation-tdd

Write a tests-first validation plan for a production ticket (and failing tests if also implementing).

Follow skill **`validation-tdd`**. Complements **`no-mistakes`** (this is before code; that is at ship). Explicit invoke only.
