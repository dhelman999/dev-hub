# IntelliJ

Primary development IDE. Cursor remains the AI / agent UI.

## Install location

Prefer documenting the real path in `docs\PATHS.md` after install.

JetBrains Toolbox often manages updates under Program Files or user AppData — that is an allowed exception to `C:\Programs`.

## What belongs in this hub

- Short notes and exported **code style** XML if you want it versioned
- Do **not** dump full project `.idea` trees here

## Related

Java formatting for agents: skill `java-coding-style` under `agent\skills\java-coding-style`.
